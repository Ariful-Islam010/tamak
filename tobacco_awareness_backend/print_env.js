const http = require('http');
http.get('http://g8ize1mukw5u8njwdxr5g1og.163.227.239.97.sslip.io/api/chat/messages', (res) => {
  // wait we can't get env vars this way unless we create an endpoint
});
